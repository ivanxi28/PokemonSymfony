{% extends 'base.html.twig' %}

{% block title %}¡Pokémon No Capturado!{% endblock %}

{% block body %}
    <div class="capture-container">
        <h1>No has capturado a {{ pokemon.name }}! 🎉</h1>

        <div class="pokemon-card">
            <img src="{{ asset('images/pokemon/' ~ pokemon.id ~ '.png') }}" alt="{{ pokemon.name }}">
            <h2>{{ pokemon.name }}</h2>
            <p>¡Intentalo en otro momento!</p>
        </div>

        <a href="{{ path('pokedex_user') }}" class="btn">Ver mi Pokédex</a>
    </div>
{% endblock %}
